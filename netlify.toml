
[build]
  command = "npm run build"
  publish = "_site"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--loglevel=warn"
  ELEVENTY_ENV = "production"

[dev]
  command = "npm run dev"
  port = 8888
  targetPort = 8080
